{
  "name": "cosmos-simulation",
  "version": "1.0.0",
  "private": false,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "audit": "node scripts/audit.mjs",
    "validate": "node scripts/validate-formulas.mjs",
    "validate:gargantua": "node scripts/validate-gargantua.mjs",
    "test": "npm run audit && npm run validate && npm run validate:gargantua && npm run build",
    "deploy": "vite build",
    "deploy:gh": "vite build && npx gh-pages -d dist"
  },
  "dependencies": {
    "lil-gui": "^0.20.0",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
